{
  "$schema": "../../../../schema/codec-vector.schema.json",
  "message_type": "goaway",
  "message_type_id": "0x10",
  "spec_section": "10.4",
  "vectors": [
    {
      "id": "empty-uri-control",
      "description": "GOAWAY on control stream with empty new session URI and timeout=0 (Request ID field removed in draft-19)",
      "hex": "1000020000",
      "decoded": {
        "new_session_uri": "",
        "timeout": "0"
      }
    },
    {
      "id": "with-uri-control",
      "description": "GOAWAY on control stream with new session URI 'moqt://relay/path' and timeout=5000ms (session migration)",
      "hex": "100014116d6f71743a2f2f72656c61792f706174689388",
      "decoded": {
        "new_session_uri": "moqt://relay/path",
        "timeout": "5000"
      }
    },
    {
      "id": "request-stream-empty-uri",
      "description": "GOAWAY on an individual request stream with empty new session URI and timeout=1000ms (migrate this request in the current session). In draft-19, control-stream and request-stream GOAWAY share one format.",
      "hex": "1000030083e8",
      "decoded": {
        "new_session_uri": "",
        "timeout": "1000"
      }
    },
    {
      "id": "request-stream-with-uri",
      "description": "GOAWAY on an individual request stream with URI and timeout=0 (migrate this request to the given URI)",
      "hex": "10001a186d6f71743a2f2f6f746865722e6578616d706c652e636f6d00",
      "decoded": {
        "new_session_uri": "moqt://other.example.com",
        "timeout": "0"
      }
    },
    {
      "id": "non-canonical-uri-length-control",
      "description": "non-canonical GOAWAY — URI length (0) is encoded as a 2-byte varint (8000) instead of the canonical 1-byte form (00); decoders MUST accept",
      "hex": "100003800000",
      "canonical": false,
      "decoded": {
        "new_session_uri": "",
        "timeout": "0"
      }
    },
    {
      "id": "truncated",
      "description": "truncated GOAWAY — missing URI length",
      "hex": "100001",
      "error": "incomplete",
      "error_detail": "unexpected end of input while reading new_session_uri length"
    }
  ]
}
